sqlite-vec v0.1.9 (source e9f598abfa0c06b328d8fe5da9c3760cce74be10), the amalgamated
`sqlite-vec.c` / `sqlite-vec.h` from the release, unmodified.

By Alex Garcia, https://github.com/asg017/sqlite-vec — dual-licensed MIT or Apache-2.0.

`../sqlite/` holds `sqlite3.h` and `sqlite3ext.h` from SQLite 3.51.2 (public domain), needed
only to compile the extension; the SQLite that runs is the one in androidx.sqlite's bundled
driver.
